{
  "scripts": {
    "jade": "jade -w -p lib/*.jade -o ./public",
    "styl": "stylus -u nib -c -w lib/app.styl -o public/css",
    "serve": "http-server .",
    "babel": "babel --watch lib --out-dir public"
  }
}